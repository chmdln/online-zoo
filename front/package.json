{
  "name": "online-zoo-front",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "engines": {
    "node": "20.x"
  },
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "start": "vite preview --host 0.0.0.0 --port $PORT"
  },
  "devDependencies": {
    "vite": "^8.0.1"
  },
  "dependencies": {
    "emoji-mart": "^5.6.0",
    "socket.io-client": "^4.8.3"
  }
}
